# This dependency tree (React 19 + several libs whose peer ranges still cap at
# React 18, e.g. react-latex-next) only resolves with legacy peer handling.
# Making it the default so every install — local, CI, and Vercel — succeeds
# without having to pass --legacy-peer-deps manually.
legacy-peer-deps=true
